A[12] = h + A[8];
h আছে $s2 তে, A এর base address s3।

lw  $t0, 32($s3)    # মেমোরির A[8] থেকে মানটি অস্থায়ী রেজিস্টার $t0-তে লোড করা হলো
add $t1, $s2, $t0    # h ($s2) এবং A[8] ($t0) যোগ করে যোগফলটি $t1 রেজিস্টারে রাখা হলো
sw  $t1, 48($s3)    # যোগফল ($t1) মেমোরির A[12] অবস্থানে স্টোর বা সংরক্ষণ করা হলো
