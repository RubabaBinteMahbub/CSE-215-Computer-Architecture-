If(i==j && i==k)
g[8] = i+j;
else
c[2] = i+k;
i আছে $s0 রেজিস্টরে
j আছে $s1 রেজিস্টরে
k আছে $s2 রেজিস্টরে
g এর বেস অ্যাড্রেস $s3
c এর বেস অ্যাড্রেস $s4

# ১. প্রথম কন্ডিশন পরীক্ষা (if i == j)
bne  $s0, $s1, Else     # যদি i এবং j অসমান (Not Equal) হয়, তবে সরাসরি Else-এ যাও

# ২. দ্বিতীয় কন্ডিশন পরীক্ষা (if i == k)
bne  $s0, $s2, Else     # যদি i এবং k অসমান (Not Equal) হয়, তবে সরাসরি Else-এ যাও

# ৩. If ব্লকের কাজ (উভয় শর্ত সত্য হলে এখানে আসবে): g[8] = i + j;
add  $t0, $s0, $s1      # $t0 = i + j
sw   $t0, 32($s3)       # g[8] = $t0 (মেমোরিতে স্টোর করা হলো)
j    Exit               # Else ব্লক স্কিপ করার জন্য Exit-এ জাম্প করো

Else:
# ৪. Else ব্লকের কাজ: c[2] = i + k;
add  $t1, $s0, $s2      # $t1 = i + k
sw   $t1, 8($s4)        # c[2] = $t1 (মেমোরিতে স্টোর করা হলো)

Exit:
# প্রোগ্রাম শেষ
