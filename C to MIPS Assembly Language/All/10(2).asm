If(i==j || i==k)
g[8] = i+j;
else
c[2] = i+k;
i আছে $s0 রেজিস্টরে
j আছে $s1 রেজিস্টরে
k আছে $s2 রেজিস্টরে
g এর বেস অ্যাড্রেস $s3
c এর বেস অ্যাড্রেস $s4

# ১. প্রথম কন্ডিশন পরীক্ষা (if i == j)
beq  $s0, $s1, If_Block   # যদি i এবং j সমান (Equal) হয়, তবে সরাসরি If_Block লেবেলে চলে যাও

# ২. দ্বিতীয় কন্ডিশন পরীক্ষা (if i == k) - প্রথমটি মিথ্যা হলে কেবল এখানে আসবে
beq  $s0, $s2, If_Block   # যদি i এবং k সমান (Equal) হয়, তাহলেও If_Block-এ চলে যাও

# ৩. Else ব্লকের কাজ (দুটি শর্তই মিথ্যা হলে কোডটি এখানে চলে আসবে): c[2] = i + k;
add  $t1, $s0, $s2        # $t1 = i + k
sw   $t1, 8($s4)          # c[2] = $t1 (মেমোরিতে স্টোর করা হলো)
j    Exit                 # Else-এর কাজ শেষ, তাই ইফ ব্লককে স্কিপ করতে Exit-এ জাম্প করো

If_Block:
# ৪. If ব্লকের কাজ: g[8] = i + j;
add  $t0, $s0, $s1        # $t0 = i + j
sw   $t0, 32($s3)         # g[8] = $t0 (মেমোরিতে স্টোর করা হলো)

Exit:
# প্রোগ্রাম শেষ

# bne দিয়ে করলে
# ১. প্রথম কন্ডিশন পরীক্ষা (if i != j হয়, তবে দ্বিতীয় কন্ডিশন দেখতে যাও)
bne  $s0, $s1, Check_Second   # i এবং j অসমান হলে দ্বিতীয় কন্ডিশন চেক করতে লাফ দাও
j    If_Block                  # আর যদি সমান হয়, তবে সরাসরি If_Block-এ চলে যাও

Check_Second:
# ২. দ্বিতীয় কন্ডিশন পরীক্ষা (if i != k হয়, তবে দুটি শর্তই মিথ্যা, তাই Else-এ যাও)
bne  $s0, $s2, Else            # i এবং k-ও অসমান হলে সরাসরি Else ব্লকে চলে যাও

If_Block:
# ৩. If ব্লকের কাজ: g[8] = i + j;
add  $t0, $s0, $s1             # $t0 = i + j
sw   $t0, 32($s3)              # g[8]-এ স্টোর করা হলো
j    Exit                      # Else ব্লক স্কিপ করার জন্য Exit-এ জাম্প করো

Else:
# ৪. Else ব্লকের কাজ: c[2] = i + k;
add  $t1, $s0, $s2             # $t1 = i + k
sw   $t1, 8($s4)               # c[2]-এ স্টোর করা হলো

Exit:
# প্রোগ্রাম শেষ
